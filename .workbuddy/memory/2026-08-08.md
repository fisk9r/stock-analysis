# 2026-08-08 工作记录

## A股盘后分析系统 — 6项需求 + 竞价前推送 全部落地

用户反馈"一条都没解决"，逐条重新排查并修复，最终全部通过构建+七视图校验+重新部署。

### 修复清单
1. **量能异动预警**：引擎 `auction_profile` 离线估算竞价量能（放量/缩量/一字锁仓/高开低走派发），挂 `vol_anomaly` 字段；`break_risk`/`recommend` 消费；前端概览 chip + 竞价视图「量能异动预警」卡。实测标记 25 只。
2. **消息推送（微信优先）**：`notifier.py` 支持 微信(ServerChan/PushPlus/企业微信机器人)/Telegram/SMTP；无通道时落地 `dist/last_push_*.md` + `push_log.jsonl` 文件痕迹（避免"啥都看不到"）。`config/notify.json` + `.example` 已建。
3. **历史连板衰减校准**：`compute_regime` 改用 `bars` 派生近20日空间高度序列，检测「峰值后衰减通道」（华电辽能8→5→4→3规律）；当前实测 8板峰值→4板，研判"高位见顶风险"因子0.43，抬高断板概率、压低买入价值分；`rec_history` 持续落库。
4. **UI重做**：`charts.js` 用 `getComputedStyle` 解析真实主题色内联（修复科技模式黑字黑底看不清）；删除硬编码 `#8a5a12`；`styles.css` 主流系统字体栈 + 移动端 `@media 768/480` 响应式。
5. **多模型综合**：`ai_judge.py` 多模型适配层（宿主Hy3 + OpenAI兼容 DeepSeek/Kimi/Qwen），`config/models.json` 可配；当前仅 Hy3 驱动（n_models=1），配密钥后自动出现「多模型共识」卡。
6. **外围市场（美股/日股/韩股）**：`em_api.global_index_snapshot` 重写——美股走腾讯源（稳定），日股/韩股走东财单指数接口（本机环境境外源 Yahoo/Stooq/Sina 全被 TLS 拦截，东财间歇性限流，故带重试退避，限流时按可得数据推断）。实测美股真实数据（道琼斯+0.22%等）。
7. **竞价前推送（新增）**：`notifier` 增 `preauction` 模式（竞价前观察简报），`build.py --preauction` 读已有 data.js 推送；站点新增「消息推送记录」卡展示最近收盘后/竞价前内容。两条自动化已建。

### 自动化
- 收盘后 16:10：`automation-1786056161903`（fetch+build+Hy3叙事+推送+部署，部署后写回 deploy_url.txt）
- 竞价前 9:00：`automation-1786123514119`（build.py --preauction 推送）

### 部署
- URL：https://1bc7e055dd1041b3b08d40649533446f.gz3.agentos-app.net （CloudStudio，重建保持同 URL）
- 数据基准日 2026-08-07；题材/板块维度仍因东财 boards 接口限流为空（环境缺口，非代码 bug，叙事已如实说明）。

### 已知环境缺口
- 东财 push2/boards 间歇性限流 → 题材/指数/轮动视图暂空；日股/韩股外围数据依赖东财单指数接口，限流时仅美股可用。
- 微信推送需用户填 `config/notify.json` 的 token（1 行）方可真正送达设备；未配置时站点「消息推送记录」卡仍可见推送内容。
