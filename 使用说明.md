# 使用说明

站点：**https://fisk9r.github.io/stock-analysis/**

打开后会出现登录框，选账户、输口令即可。勾了"在本机记住口令"就只需输一次，
以后打开直接进；想换人看，点右下角"切换账户"。

---

## 一、它会自己跑，你不用管

每个交易日（北京时间）自动执行，无需开电脑：

| 时间 | 干什么 |
|---|---|
| 09:02 | 盘前预判推送 |
| 09:25 | 集合竞价判断推送 |
| 10:07 / 11:07 / 13:07 / 14:07 / 15:07 | 盘中异动推送 |
| **16:10** | **收盘完整分析 + 更新网站** |
| 20:00 | 复盘补发 |
| 周日 20:00、周一 08:00 | 周末发酵消息（有料才发） |

网站内容在每天 16:10 那一轮更新。其余时段只发推送，不动网站。

---

## 二、加人 / 删人（你自己控制谁能看）

在项目目录下开命令行（cmd 或 PowerShell），执行：

```bat
:: 看看现在有谁
python tools/manage_users.py list

:: 加一个人，口令自动生成（把生成的口令发给他）
python tools/manage_users.py add --name 张三

:: 加一个人，自己指定口令
python tools/manage_users.py add --name 李四 --pass 李四的口令2026

:: 删掉某人（id 从 list 里看）
python tools/manage_users.py remove --id friend1

:: 给自己换个口令
python tools/manage_users.py passwd --id owner
```

改完会自动同步到云端并触发重建，**约 2 分钟后生效**。
被删的人，他那份加密文件会从网站上消失，旧口令再也打不开。

> 前提：仓库外那个 `.ghtoken` 文件还在（存着 GitHub 令牌）。
> 如果你已经撤销了令牌，就新建一个再放回去，或者告诉我，我来处理。

---

## 三、数据是怎么保密的

网站本身是公开的，但**上面没有任何明文数据**：

- 每个人一份独立密文 `data/<id>.bin`，用他自己的口令加密
  （PBKDF2-HMAC-SHA256 二十万次迭代派生密钥 + HMAC 密钥流异或）
- 明文 `data.js` 被明确排除在发布内容之外，访问它是 404
- 口令只存在你本机和 GitHub 的加密密钥里，网页上连用户列表都不含口令

所以链接被人转发出去也没关系——没有口令，下载到的就是一堆乱码。

---

## 四、数据准不准：多源交叉校验

以前只从东方财富取数，偶尔会有错。现在每次分析都会**同时抓东方财富、新浪、腾讯三家**，
对涨停股、推荐股、妖股逐只比对：

- 三家一致 → 采信
- 有分歧且离散度超过 0.5% → 取**中位数**为准，并在该股名字旁标一个 **⚠存疑** 徽标
- 网站"市场概览"里有一块"数据质量"，写明本次抽检多少只、几只存疑、分别是哪几只

看到 ⚠ 就知道那只票的数据源打架了，自己再核一眼。

---

## 五、出问题时怎么查

```bat
:: 云端现状：数据快照、Pages、最近几次运行
python tools/gh_status.py

:: 盯住最近一次运行，失败会告诉你挂在哪一步
python tools/gh_watch.py

:: 体检线上站点：该有的在不在、明文有没有泄露
python tools/verify_site.py https://fisk9r.github.io/stock-analysis
```

也可以直接看：https://github.com/fisk9r/stock-analysis/actions

---

## 六、安全提醒

- **令牌用完就撤**：GitHub → Settings → Developer settings → Personal access tokens
  → 把这次用的那个 token 点 Delete。撤销后网站照常自动运行（云端用的是 GitHub 自带的临时凭据），
  只是本机的 `manage_users.py` 暂时用不了，需要时再建一个即可。
- `config/allowed_users.json`、`config/owner_pass.txt`、`.ghtoken` 这三个文件
  **绝不会进入 Git 仓库**（已在 .gitignore 中排除），只在你本机。
